import React from 'react';
import { DesktopManager } from '@/components/DesktopManager';

/**
 * Desktop Page
 * 
 * Renders the CraftingTable OS desktop environment with the Universal Emulator app
 */
export default function Desktop() {
  return (
    <DesktopManager />
  );
}